syntax = "proto3";

package adder;

option go_package = "somon.adder.v1;adder";

service Adder {
    rpc Add(AddRequest) returns (AddResponse) {}
}

message AddRequest {
  int64 x = 1;
  int64 y = 2;
}

message AddResponse {
  int64 result = 1;
}